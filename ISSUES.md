# Starter Issues for Dev Analytics Dashboard

## Good First Issue — Add script to fetch PRs for a repo
- Implement a script that fetches recent PRs for a given repo using GitHub API and stores them in JSON.
- Labels: `good first issue`, `backend`, `github`

## Help Wanted — Create contributor leaderboard API
- Aggregate contributions and expose `/leaderboard` endpoint.
- Labels: `help wanted`, `backend`, `analytics`

## Bug — Rate limit errors on GitHub API
- Implement exponential backoff and caching to handle rate limits.
- Labels: `bug`, `backend`
